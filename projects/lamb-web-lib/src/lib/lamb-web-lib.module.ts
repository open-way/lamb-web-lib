import { NgModule } from '@angular/core';
import { LambWebLibComponent } from './lamb-web-lib.component';

@NgModule({
  imports: [
  ],
  declarations: [LambWebLibComponent],
  exports: [LambWebLibComponent]
})
export class LambWebLibModule { }
